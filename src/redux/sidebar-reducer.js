let initialState = {
  friends: [
    { id: 1, name: "Friendo" },
    { id: 2, name: "Capybara" },
    { id: 3, name: "Kruglov" },
  ]
}

const sidebarReducer = (state = initialState, action) => {
  return state;
};

export default sidebarReducer;
